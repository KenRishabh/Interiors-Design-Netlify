[build]
  command = "npm run dev && yarn dev && pnpm dev && npm run build" # Replace with your actual build command
  publish = "components/HeroSection.jsx" # Replace with the directory containing your built files

  # Example of using caching
  cache = [
    "node_modules",
    "package-lock.json",
  ]
